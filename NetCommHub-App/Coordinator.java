// Represents a message indicating the role of a coordinator in a system
public class Coordinator extends Carrier {

    // Constructor for Coordinator class
    public Coordinator() {
        // Call the superclass constructor (Carrier) to initialize common fields
        super();
    }
}